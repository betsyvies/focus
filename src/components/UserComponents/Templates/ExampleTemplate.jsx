import React, { Component } from 'react';

/* Renderiza el plugin grapes para hacer las plantillas */
class ExampleTemplate extends Component {
  render() {
    return (
      <div id="gjs" style={{ height: '0px', overflow: 'hidden' }}>
      </div>
    );
  }
}

export default ExampleTemplate;